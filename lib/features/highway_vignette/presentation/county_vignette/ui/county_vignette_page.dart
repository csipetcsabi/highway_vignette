import 'package:flutter/material.dart';

class CountyVignettePage extends StatelessWidget {
  const CountyVignettePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('County Vignette Page'),
      ),
    );
  }
}